import React, {Component} from 'react';

import './ConversationsButton.css';

class ConversationsButton extends Component {
    render() {
        return (
            <button className="ConversationsButton">+ New coversation</button>
        )
    }
}

export  default ConversationsButton;